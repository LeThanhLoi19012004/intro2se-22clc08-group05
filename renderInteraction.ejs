<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Post Interaction</title>
</head>
<body>
  <h1>Post Interaction</h1>
  <div>
    <% comments.forEach(comment => { %>
      <div>
        <p><strong>Full Name:</strong> <%= comment.fullname %></p>
        <p><strong>Comment:</strong> <%= comment.comment %></p>
        <% if (comment.avatar && comment.avatar.filename) { %>
          <img src="/uploads/<%= comment.avatar.filename %>" alt="Avatar" style="width:50px;height:50px;">
        <% } else { %>
          <p>No Avatar</p>
        <% } %>
      </div>
      <hr>
    <% }) %>
  </div>
</body>
</html>