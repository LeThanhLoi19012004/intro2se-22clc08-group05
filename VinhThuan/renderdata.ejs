<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Event Information</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
    }
    h1 {
      color: #333;
    }
    ul {
      list-style-type: none;
      padding: 0;
    }
    li {
      border: 1px solid #ddd;
      padding: 10px;
      margin-bottom: 10px;
    }
    img {
      max-width: 100%;
      height: auto;
      display: block;
    }
  </style>
</head>
<body>
  <h1>Event Information</h1>
  <% if (!events || events.length === 0) { %>
    <p>No events found.</p>
  <% } else { %>
    <ul>
      <% events.forEach(event => { %>
        <li>
          <p><strong>Name:</strong> <%= event.eventname %></p>
          <p><strong>Type:</strong> <%= event.eventtype %></p>
          <p><strong>Description:</strong> <%= event.descriptionevent %></p>
          <p><strong>Date:</strong> <%= event.eventdate ? new Date(event.eventdate).toDateString() : 'No date available' %></p>
          <p><strong>Time:</strong> <%= event.eventtime.hour %>:<%= String(event.eventtime.minute).padStart(2, '0') %> <%= event.eventtime.amPm %></p>
          <p><strong>Total Tickets:</strong> <%= event.numberoftickets %></p>
          <p><strong>Ticket Available:</strong> <%= event.ticketavailable %></p>
          <p><strong>Price:</strong> <%= event.price %></p>
          <p><strong>Owner:</strong> <%= event.profile ? event.profile.fullname : 'Unknown' %></p>
          <p><strong>Rules:</strong> <%= event.rulesevent %></p>
          <p><strong>Location:</strong> <%= event.location ? event.location : 'Unknown' %></p>
          <p><strong>Ticket Type:</strong> <%= event.tickettype %></p>
          <% if (event.logoevent && event.logoevent.length > 0) { %>
            <p><strong>Images:</strong></p>
            <% event.logoevent.forEach(logo => { %>
              <img src="/uploads/<%= logo.filename %>" alt="Event Image">
            <% }) %>
          <% } else { %>
            <p>No images available</p>
          <% } %>
        </li>
      <% }) %>
    </ul>
  <% } %>

  <script>
    // You can add JavaScript here if needed
  </script>
</body>
</html>
